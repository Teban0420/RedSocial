
<main class="contenedor contenedor-formularios no-padding">
    <h1><%= pagina %></h1>
    <form class="default-form" action="/nuevo-grupo" method="POST" enctype="multipart/form-data">
        
        <div class="campo">
            <label>Nombre</label>
            <input type="text" name="nombre" placeholder="Nombre del Grupo">
        </div>

        <div class="descripcion">
            <label>Descripcion</label>
            <input type="hidden" id="x" name="descripcion">
            <trix-editor input="x"></trix-editor>
        </div>

        <div class="campo">
            <label>Categoria</label>
            <select name="categoria" id="">
                <option value="" selected disabled>-- Elige una categoria</option>
                <% categorias.forEach( categoria => { %>
                    <option value="<%= categoria.id %>"><%= categoria.nombre %></option>
                <% }) %>
            </select>
        </div>

        <div class="campo">
            <label>Imagen</label>
            <input type="file" name="imagen">
        </div>

        <div class="campo">
            <label>Sitio Web</label>
            <input type="url" name="url" placeholder="Tu sitio web">
        </div>

        <div class="campo">
            <input type="submit" value="Crear Grupo" class="btn btn-rosa">
        </div>
    </form>
</main>