<div class="contenedor proximos-eventos">
    <h1><%= pagina %></h1>
    <% if(meetis.length > 0) { %>
        <div class="grid columnas-md-3 ">
            <% meetis.forEach( meeti => { %>
                <%- include('card.ejs', {meeti : meeti}) %>
            <% }) %>
        </div>
    <% } else { %>
        <p>No hay meeti's en esta categoria</p>
    <% } %>
</div>